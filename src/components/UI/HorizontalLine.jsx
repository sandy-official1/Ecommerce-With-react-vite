const HorizontalLine = ({ ...rest }) => {
  return (
    <div
      {...rest}
      style={{
        borderTop: "1.5px solid rgb(234, 234, 236)",
      }}
    />
  );
};

export default HorizontalLine;
